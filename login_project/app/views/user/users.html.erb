<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Products</title>
  </head>
  <body>
    <div class="container">
  <h2>Showing All Users</h2>
  <table class="table">
    <thead>
      <tr>
        <th>User ID</th>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Email</th>
        <th>Secrets</th>
        <th>Likes</th>
        <th>Options</th>
      </tr>
    </thead>

    <tbody>
      <% @users.each do |user| %>

      <tr>
        <td><%=user.id%></td>
        <td><%=user.fname%></td>
        <td><%=user['lname']%></td>
        <td><%=user[:email]%></td>
        <td><%=user.secrets.count%></td>
        <td><%=user.likes.count%></td>
        <td><a href="/show/<%=user.id%>">Show</a> | <a href="/edit/<%=user.id%>">Edit</a> | <a href="/destroy/<%=user.id%>">Destroy</a></td>
      </tr>

      <% end %>
    </tbody>
  </table>
  <br>
  <br>
  <a href="/">Create a New User</a> | <a href="/logout">Logout</a> | <a href="/allsecrets">All Secrets</a>
</div>


  </body>
</html>
